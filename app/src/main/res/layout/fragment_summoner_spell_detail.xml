<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nested_scroll_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/empty_view_tv"
            style="@style/EmptyText"
            android:layout_centerInParent="true"
            android:text="@string/summoner_spell_not_found"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/summoner_spell_detail_image"
                style="@style/Image"
                tools:src="@drawable/ic_launcher_foreground" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/summoner_spell_detail_name"
                    style="@style/TitleText"
                    tools:text="Cleanse" />

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/summoner_spell_detail_cooldown_label"
                        style="@style/LabelText"
                        android:visibility="invisible"
                        android:text="@string/cooldown" />

                    <TextView
                        android:id="@+id/summoner_spell_detail_cooldown"
                        style="@style/BoldText"
                        tools:text="25325" />

                </LinearLayout>

                <TextView
                    android:id="@+id/summoner_spell_detail_description"
                    style="@style/BoldText"
                    tools:text="Makes you cleaner." />

            </LinearLayout>

        </LinearLayout>

    </RelativeLayout>

</android.support.v4.widget.NestedScrollView>